<?php
/**
 * WooCommerce runtime constants — IDE stubs only.
 *
 * This file is scanned by Intelephense/PHPStan so the IDE knows these constants
 * exist at runtime. It is NEVER loaded by PHP directly (not autoloaded,
 * not required). The defined() guard prevents any conflict if it were ever
 * accidentally included while WooCommerce is already active.
 */

defined('WC_VERSION') || define('WC_VERSION', '9.0.0');
defined('WC_ABSPATH') || define('WC_ABSPATH', '');
defined('WC_PLUGIN_FILE') || define('WC_PLUGIN_FILE', '');
